{
  "name": "@halospv3/hce.shared-config",
  "version": "1.0.5",
  "description": "Automate commit message quality, changelogs, and CI/CD releases. Exports a semantic-release shareable configuration deserialized from this package's '.releaserc.yml'. Shared resources for .NET projects are also distributed with this package.",
  "keywords": [
    "halo",
    "halo-ce",
    "semantic-release",
    "semantic-release-config"
  ],
  "type": "module",
  "homepage": "https://github.com/halospv3/hce.shared#readme",
  "bugs": "https://github.com/halospv3/hce.shared/issues",
  "license": "MIT",
  "author": "Noah Sherwin",
  "files": [
    "!tests/",
    "CHANGELOG.md",
    "cjs/**/*",
    "dotnet/",
    "src/**/*",
    "static/**/*"
  ],
  "main": "./cjs/index.cjs",
  "infra": "polyrepo",
  "repository": "github:halospv3/hce.shared",
  "scripts": {
    "build": "packemon build",
    "check": "npm run type && npm run test && npm run lint",
    "clean": "packemon clean",
    "coverage": "npm run test --coverage",
    "format": "prettier --write \"./src/**/*\" \"./tests/**/*\"",
    "lint": "eslint --cache --fix \"./src/**/*\" \"./tests/**/*\"",
    "pack": "packemon pack --addEngines --addFiles --declaration",
    "prepare": "husky install",
    "presemantic-release": "npm run pack && npm run check",
    "semantic-release": "npx semantic-release",
    "test": "npm run build && node --loader tsx --no-warnings --test ./tests/index.test.mts",
    "type": "tsc",
    "validate": "packemon validate",
    "watch": "packemon watch"
  },
  "dependencies": {
    "@commitlint/cli": "^17.7.1",
    "@commitlint/config-conventional": "^17.7.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^10.0.4",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^9.0.4",
    "@semantic-release/npm": "^11.0.0",
    "@semantic-release/release-notes-generator": "^11.0.4",
    "ajv": "^8.12.0",
    "ajv-draft-04": "^1.0.0",
    "conventional-changelog-conventionalcommits": "^7.0.2",
    "husky": "^8.0.3",
    "js-yaml": "^4.1.0",
    "node-fetch": "^3.3.2",
    "semantic-release": "^22.0.0"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^18.12.4",
    "@types/debug": "^4.1.8",
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^20.5.1",
    "@types/semantic-release": "^20.0.1",
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "@typescript-eslint/parser": "^6.7.5",
    "babel-cli": "^6.26.0",
    "babel-plugin-cjs-esm-interop": "^3.0.2",
    "eslint": "^8.51.0",
    "eslint-config-moon": "^2.0.10",
    "eslint-plugin-disable": "^2.0.3",
    "node-fetch": "^3.3.2",
    "packemon": "^3.2.1",
    "prettier": "^3.0.3",
    "prettier-config-moon": "^1.1.2",
    "tsconfig-moon": "^1.3.0",
    "tslib": "^2.6.2",
    "tsx": "^3.13.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.12.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "tag": "latest"
  },
  "exports": {
    ".": {
      "node": {
        "import": "./index-wrapper.mjs",
        "types": "./index.d.ts",
        "require": "./index.cjs"
      }
    }
  },
  "types": "./cjs/index.d.ts",
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "packemon": {
    "format": "cjs",
    "platform": "node"
  },
  "// semantic-release": "After HCE.Shared's Shareable Configuration (see 'node/static/releaserc.yml', https://semantic-release.gitbook.io/semantic-release/usage/configuration#extends) is loaded, the following semantic-release configuration is merged into the configuration object, overriding existing values. Later, when a release is triggered, semantic-release will add any unspecified settings with default values.",
  "// semantic-release - How to review the 'actual' config": "`npx semantic-release --dry-run --debug`. This will run semantic-release with 'dry-run' (no committing changes, tags, publishing activity, and more) and 'debug' (output verbose info, including the actual config object).",
  "// semantic-release - WARNING!": "Arrays and child objects are overwritten entirely. When 'plugins' is set, only commit-analyzer is added if missing.",
  "// semantic-release - plugins configuration": "Plugins are configured via tuple-like arrays e.g. ['plugin name', {config object}].",
  "release": {
    "extends": [
      "./cjs/index.cjs"
    ],
    "plugins": [
      [
        "@semantic-release/commit-analyzer",
        {
          "// releaseRules - About": "https://github.com/semantic-release/commit-analyzer#releaserules",
          "// defaults": "https://github.com/semantic-release/commit-analyzer/blob/master/lib/default-release-rules.js",
          "releaseRules": [
            {
              "type": "(build|chore|fix)",
              "scope": "deps",
              "release": "patch"
            }
          ]
        }
      ],
      "@semantic-release/release-notes-generator",
      "@semantic-release/changelog",
      "@semantic-release/npm",
      "@semantic-release/git",
      "@semantic-release/github"
    ]
  }
}
